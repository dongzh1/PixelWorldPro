version: 1
Dimension:
  - name: nether
    Create:
      Creator: "auto"
      CreateUse: both
      CreateMoney: 100
      CreatePoints: 100
    #是否启用边界？
    #Enable boundaries?
    Barrier: true
  - name: the_end
    Create:
      Creator: "auto"
      CreateUse: both
      CreateMoney: 100
      CreatePoints: 100
    #是否启用边界？
    #Enable boundaries?
    Barrier: true